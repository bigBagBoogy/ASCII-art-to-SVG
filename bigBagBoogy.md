# instant push copy paste all below in one go:

git init
git branch -M main
git add .
git commit -m "finalized code"
git push -u origin main

# todo:⭐️

# let's:

# end of todo..........................................
